let num = 5;
let str = 'word';
let bool = true;
let bint = 234324239n;
let empty;
let book = {
    title: 'name of a book',
    author: 'someone',
    pages: 100,
};

console.log(`TASK2\n${num} is ${typeof(num)}\n${str} is ${typeof(str)}\n${bool} is ${typeof(bool)}\n${bint} is ${typeof(bint)}\n${empty} is ${typeof(empty)}\n${book} is ${typeof(book)}`);